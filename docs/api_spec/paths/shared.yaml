# =====================================================================
# paths/shared.yaml — response reusable lintas domain (401/403/404/
# 409/429), seluruhnya mereferensikan ErrorEnvelope tunggal di
# schemas/shared.yaml.
# =====================================================================

Unauthorized:
  description: Token tidak ada atau tidak valid
  content:
    application/json:
      schema:
        $ref: '../schemas/shared.yaml#/ErrorEnvelope'
      example:
        status: error
        code: 401
        message: "Token tidak valid atau sudah kedaluwarsa"

Forbidden:
  description: Token valid, tetapi tidak memiliki permission yang dibutuhkan
  content:
    application/json:
      schema:
        $ref: '../schemas/shared.yaml#/ErrorEnvelope'
      example:
        status: error
        code: 403
        message: "Anda tidak memiliki izin untuk mengakses resource ini"

NotFound:
  description: Resource tidak ditemukan
  content:
    application/json:
      schema:
        $ref: '../schemas/shared.yaml#/ErrorEnvelope'
      example:
        status: error
        code: 404
        message: "Resource tidak ditemukan"

Conflict:
  description: Permintaan bertentangan dengan aturan bisnis saat ini
  content:
    application/json:
      schema:
        $ref: '../schemas/shared.yaml#/ErrorEnvelope'
      example:
        status: error
        code: 409
        message: "Tidak dapat menonaktifkan admin terakhir yang aktif"

RateLimited:
  description: Melebihi batas percobaan (rate limit umum, atau lockout login FS-01)
  content:
    application/json:
      schema:
        $ref: '../schemas/shared.yaml#/ErrorEnvelope'
      example:
        status: error
        code: 429
        message: "Terlalu banyak percobaan gagal. Coba lagi dalam 15 menit."

BmkgUpstreamError:
  description: Gagal mengambil data dari upstream BMKG (bukan 500 - lihat API_SPEC.md v3.1 §6)
  content:
    application/json:
      schema:
        $ref: '../schemas/shared.yaml#/ErrorEnvelope'
      example:
        status: error
        code: 502
        message: "Gagal mengambil data dari BMKG, coba lagi nanti"